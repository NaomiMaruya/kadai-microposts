@if (Auth::id() != $user->id)
    @if (Auth::user()->is_following($user->id))
        {{-- アンフォローボタンのフォーム --}}
        {!! Form::open(['route' => ['user.unfollow', $user->id], 'method' => 'delete']) !!}
            {!! Form::submit( __('messages.Unfollow'), ['class' => "btn btn-outline-primary btn-block"]) !!}
        {!! Form::close() !!}
    @else
        {{-- フォローボタンのフォーム --}}
        {!! Form::open(['route' => ['user.follow', $user->id]]) !!}
            {!! Form::submit( __('messages.Follow'), ['class' => "btn btn-outline-danger btn-block"]) !!}
        {!! Form::close() !!}
    @endif
@endif